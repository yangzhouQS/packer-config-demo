import { RunRsbuildCompilerArgOptions } from "../types/compile.ts";
import { BaseCompiler } from "./base.compiler.ts";

export class RspackCompiler extends BaseCompiler {
  public run(arg: RunRsbuildCompilerArgOptions): void {
  }
}
